interface:
  display_name: "Crossexam"
  short_description: "Repo-specific patterns and workflows for crossexam"
  default_prompt: "Use the crossexam repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true